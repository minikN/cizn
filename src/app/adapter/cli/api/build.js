const build = app => async (options, command) => {
//   const { ...state } = await State({ config }, command)
//   const configPath = await realpath(config)
//   const stuff = await import(/* webpackIgnore: true */configPath)
//   console.log('stuff', stuff)
  console.log('build')
}

export default build
